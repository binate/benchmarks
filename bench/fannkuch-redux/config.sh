# Pinned parameters for the fannkuch-redux benchmark.
DEFAULT_N=11           # permutation size (12 is canonical but ~10x slower)
SMALL_N=7              # size for the CI correctness cross-check
JAVA_MAIN=FannkuchRedux
COMPARE=exact          # deterministic integer output, matched byte-for-byte
