# Pinned parameters for the richards benchmark.
DEFAULT_N=10000        # number of Richards scheduler simulations (for timing)
SMALL_N=1              # one simulation suffices for the correctness cross-check
JAVA_MAIN=Richards
COMPARE=exact          # deterministic "queue <n> hold <n>" line
