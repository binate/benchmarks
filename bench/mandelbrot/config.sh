# Pinned parameters for the mandelbrot benchmark.
DEFAULT_N=1000       # w = h = 1000 → a 125 KB P4 bitmap
JAVA_MAIN=Mandelbrot
COMPARE=bytes        # output is a packed 1-bit-per-pixel P4 bitmap
SMALL_N=200            # size for the CI correctness cross-check
